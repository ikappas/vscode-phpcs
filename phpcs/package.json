{
	"name": "phpcs",
	"description": "PHP CodeSniffer for Visual Studio Code",
	"version": "0.1.0",
	"publisher": "ikappas",
	"icon": "phpcs_icon.svg",
	"galleryBanner": {
		"color": "#6082bb",
		"theme": "dark"
	},
	"homepage": "https://github.com/ikappas/vscode-phpcs/blob/master/README.md",
	"repository": {
		"type": "git",
		"url": "https://github.com/ikappas/vscode-phpcs.git"
	},
	"bugs": {
		"url": "https://github.com/ikappas/vscode-phpcs/issues"
	},
	"categories": [
		"Languages",
		"Linters"
	],
	"engines": {
		"vscode": "0.10.x"
	},
	"activationEvents": [
		"onLanguage:php"
	],
	"main": "./out/src/extension",
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "PHP CodeSniffer configuration",
			"properties": {
				"phpcs.enable": {
					"type": "boolean",
					"default": true,
					"description": "Control whether phpcs is enabled for PHP files or not."
				},
				"phpcs.standard": {
					"type": "string",
					"default": null,
					"description": "The coding standard to use by default. This can be overriden in a phpcs.xml in your workspace."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "node ./node_modules/vscode/bin/compile",
		"compile": "node ./node_modules/vscode/bin/compile -watch -p ./"
	},
	"devDependencies": {
		"typescript": "^1.6.2",
		"vscode": "0.10.x"
	},
	"dependencies": {
		"vscode-languageclient": "0.10.x"
	}
}